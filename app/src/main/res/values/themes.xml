<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CardView" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@color/green</item>
        <item name="android:windowLightStatusBar">false</item> <!-- For dark icons/text on a light background -->
        <item name="android:colorPrimaryDark">@color/green</item>
        <item name="android:colorPrimary">@color/green</item>
    </style>

    <style name="Theme.CardView" parent="Base.Theme.CardView" />
</resources>
